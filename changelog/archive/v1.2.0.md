# v1.2.0 Quality & Hardening / 质量与健壮性升级

**Release Date / 发布日期**: 2026-04-06

---

## Overview / 概览

Major quality improvements with new modules for statistics, achievements, and modes. Enhanced accessibility and testing.

重要的质量改进，新增统计、成就和模式模块。增强可访问性和测试。

---

## Added / 新增

### New Modules / 新模块

| Module / 模块            | File / 文件            | Purpose / 职责                              |
| ------------------------ | ---------------------- | ------------------------------------------- |
| Stats / 统计             | `src/stats.js`         | Statistics aggregation / 统计累计           |
| Achievements / 成就      | `src/achievements.js`  | Achievement definitions / 成就定义          |
| Modes / 模式             | `src/modes.js`         | N-back and recall logic / N-back 和回忆逻辑 |
| Import/Export / 导入导出 | `src/import-export.js` | Data normalization / 数据规范化             |

### New Tests / 新测试

| Test File / 测试文件    | Coverage / 覆盖               |
| ----------------------- | ----------------------------- |
| `storage.test.js`       | Storage module / 存储模块     |
| `timer.test.js`         | Timer module / 计时器         |
| `i18n.test.js`          | Internationalization / 国际化 |
| `import-export.test.js` | Import/Export / 导入导出      |
| `modes.test.js`         | Training modes / 训练模式     |

---

## Changed / 变更

### src/storage.js

- Unified normalized read/write / 统一规范化读写
- Settings, stats, achievements now validated / 设置、统计、成就验证收紧

### src/timer.js

- Timestamp-based calculation / 基于时间戳计算
- Reduced drift from `setInterval(1000)` / 减少计时漂移

### app.js

- Integrated new modules / 接入新模块
- ARIA accessibility improvements / 可访问性改进
  - Cards: `aria-label`, `aria-pressed` / 卡牌属性
  - Modals: `aria-hidden`, focus restoration / 弹窗属性

### index.html

- Adjusted script loading order / 调整脚本加载顺序

---

## Fixed / 修复

- `.gitattributes` glob syntax / glob 语法问题
- `.gitignore` `.vscode/` tracking conflict / `.vscode/` 追踪冲突
- `.vscode/tasks.json` non-existent script reference / 不存在脚本引用

---

## Impact Scope / 影响范围

- Game logic / 游戏运行时: `app.js`
- Persistence / 持久化: `storage.js`, `import-export.js`
- Stats/Achievements/Modes / 统计/成就/模式: `stats.js`, `achievements.js`, `modes.js`
- Timer / 计时器: `timer.js`

---

## Verified / 验证

- `npm test`: ✅ Passed / 通过
- `npm run lint`: ✅ Passed / 通过
- `npm run build:css`: ✅ Success / 成功
- `npm run prepare:deploy`: ✅ Success / 成功

---

## Project Status / 项目状态

Significant quality and robustness improvements with higher modularity and testability.

代码质量和健壮性大幅提升，模块化程度更高，可测试性更强。
