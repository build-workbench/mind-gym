# v1.2.1 Code Optimization / 代码优化

**Release Date / 发布日期**: 2026-04-15

---

## Overview / 概览

Code optimization with internationalization improvements, test additions, and bug fixes.

代码优化，完善国际化，添加测试，修复 Bug。

---

## Fixed / 修复

| Issue / 问题                                         | Fix / 修复方式                                           |
| ---------------------------------------------------- | -------------------------------------------------------- |
| `.gitattributes` invalid syntax / 无效语法           | Per-line patterns instead of glob / 每行一个模式         |
| `.gitignore` contradiction / 矛盾                    | Removed `.vscode/` exclusion / 移除 `.vscode/` 排除      |
| `.vscode/tasks.json` missing script / 引用不存在脚本 | Replaced `build` with `format` / `build` 替换为 `format` |
| `importDataFromObj` empty catch / 空 catch 块        | Added `logError` logging / 添加错误日志                  |

---

## Added / 新增

### Internationalization / 国际化

- Achievement definitions use i18n keys / 成就定义使用 i18n 键
- Achievement unlock notifications localized / 成就解锁提示国际化
- Best score units localized / 最佳成绩单位国际化
- 14 new i18n keys (EN/ZH) / 新增 14 个 i18n 键

### Testing / 测试

- `__tests__/helpers.test.js`: 7 test cases / 7 个测试用例
- Coverage: DEFAULT_SETTINGS, adaptiveKey, loadAdaptive, getAdaptiveAssist

### Package Metadata / 包元数据

```json
{
  "description": "Browser-based memory training game...",
  "keywords": ["memory", "training", "pwa"],
  "author": "LessUp",
  "license": "MIT",
  "homepage": "https://lessup.github.io/mind-gym/",
  "repository": "https://github.com/LessUp/mind-gym.git"
}
```

---

## Changed / 变更

### Code Refactoring / 代码重构

| Optimization / 优化                              | Description / 说明                                      |
| ------------------------------------------------ | ------------------------------------------------------- |
| Extract `showModal`/`hideModal` / 提取模态框函数 | Replaced ~20 repetitive patterns / 替换约 20 处重复模式 |
| Extract `buildDeckItems` / 提取牌组构建          | Eliminated duplicate deck building logic / 消除重复逻辑 |

---

## Verified / 验证

- `npm test`: All tests passed / 全部测试通过

---

## Project Status / 项目状态

Cleaner code, complete internationalization, improved code quality.

代码更简洁，国际化更完整，工程质量进一步提升。
