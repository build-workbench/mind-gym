# 2025-12-18

## 新增
- 新增 `changelog/` 目录并开始记录变更。
- 新增 `LICENSE`（MIT）。
- 新增 `docs/` 文档骨架：
  - `docs/README.md`
  - `docs/architecture.md`
  - `docs/storage.md`
  - `docs/modes.md`
  - `docs/pwa.md`
- 新增 PWA 图标资源 `assets/icon.svg`。
- 新增无构建模块：
  - `src/keys.js`：集中管理 localStorage key 生成。
  - `src/utils.js`：集中管理随机/洗牌等通用工具。

## 变更
- 更新 `manifest.webmanifest`：补齐 `icons` 声明。
- 更新 `sw.js`：预缓存列表加入 `assets/icon.svg`。
- 更新 `sw.js`：预缓存列表加入 `src/keys.js` 与 `src/utils.js`。
- 更新 `index.html`：在加载 `app.js` 前加载 `src/keys.js` 与 `src/utils.js`。
- 更新 `app.js`：接入 `RememberKeys` / `RememberUtils`，并改为通过模块生成 key 与随机/洗牌。
- 更新 `scripts/prepare-deploy.sh`：部署打包时复制 `src/` 目录到 `dist/`。

## 修复
- 修复 `sw.js` 在 activate 阶段误删运行时 CDN 缓存的问题（保留 `runtime-cdn-v1`），避免离线样式缓存被清理。
- 修复 `index.html` 中 Tailwind CDN 与 `tailwind.config` 的执行顺序，避免 `tailwind` 未定义导致暗色 class 模式配置不生效。


